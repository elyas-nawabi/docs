post:
      tags:
        - tax
      summary: Test tax.
      description: Test tax.
      operationId: postTaxTest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              $ref: 'schemas/create-tax-test.yaml'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                  type: array
                  properties:
                    data: 
                      type: array
                    errors:
                      type: object
                    pagination:
                      type: object
                    status:
                      type: integer
                    success_message:
                      type: string
              example:
                data: []
                errors: null
                pagination: null
                status: 200
                success_message: null
        '401':
          description: >-
            401 Unauthorized: The server could not verify that you are
            authorized to access the URL requested. You either supplied the
            wrong credentials (e.g. a bad password), or your browser doesn't
            understand how to supply the credentials required.
          content:
            application/json:
              schema:
                $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'
    