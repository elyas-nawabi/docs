post:
  tags:
    - headcount_working_rates
  summary: Create Headcount Working Rate.
  description: Create Headcount Working Rate.
  operationId: createHeadcountworkingrates
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: 'schemas/create-headcount-working-rates.yaml'
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: 'schema.yaml'
    '401':
      description: Unauthorized.
      content:
        application/json:
          schema:
            $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'


delete:
  tags:
    - headcount_working_rates
  summary: Delete Headcount Working Rate.
  description: Delete one or more Headcount Working Rate by id.
  operationId: deleteHeadcountworkingrates
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            ids:
              type: array
              items:
                type: integer
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../../api_doc.yaml#/components/schemas/HTTP200'
    '401':
      description: Unauthorized.
      content:
        application/json:
          schema:
            $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'

