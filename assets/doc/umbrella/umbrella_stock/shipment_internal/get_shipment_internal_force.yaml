    get:
      tags:
        - shipment internal
      summary: Force assign shipment internal.
      description: Force assign shipment internal.
      operationId: forceShipmentInternalById
      parameters:
        - name: obj_id
          in: path
          schema:
            type: integer
          rquired: true
          example: 1
      requestBody:
        description: add session_id.
        content:
          application/json:
            schema:
              type: object
              properties:
                session_id: 
                  type: integer
            example:
              session_id: 21
      responses:
        '200':
          description: OK
          content:
            application/json:
              data:
                type: object
                nullable: true
              errors:
                type: object
                nullable: true
              pagination:
                type: object
                nullable: true
              status:
                type: integer
              success_message:
                type: string
                nullable: true
            example:
              errors: null
              pagination: null
              status: 201
              success_message: null

        '401':
          description: >-
            401 Unauthorized: The server could not verify that you are
            authorized to access the URL requested. You either supplied the
            wrong credentials (e.g. a bad password), or your browser doesn't
            understand how to supply the credentials required.
          content:
            application/json:
              schema:
                $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'
        '404':
          description: 404 Not Found.
          content:
            application/json:
              schema:
                $ref: '../../../api_doc.yaml#/components/schemas/HTTP404'
    