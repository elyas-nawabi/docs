    post:
      tags:
        - module
      summary: Start upgrade.
      description: Start upgrade.
      operationId: getStartUpgradeById
      parameters:
        - name: obj_id
          in: path
          schema:
            type: integer
          rquired: true
          example: 1
      requestBody:
        schema:
          properties:
            session_id:
              type: integer
        required:
          - session_id
        example:
          session_id: 1
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: 
                    type: object
                    properties:
                      modules: 
                        type: string
                      session_id:
                        type: integer
                  error: 
                    type: object
                  pagination:
                    type: object
                  status: 
                    type: integer
                  success_message:
                    type: object
              example:
                data: 
                  modules: umbrella_stock
                  session_id: 74
                error: null
                pagination: null
                status: 200
                success_message: null
        '401':
          description: >-
            401 Unauthorized: The server could not verify that you are
            authorized to access the URL requested. You either supplied the
            wrong credentials (e.g. a bad password), or your browser doesn't
            understand how to supply the credentials required.
          content:
            application/json:
              schema:
                $ref: '../../../../api_doc.yaml#/components/schemas/HTTP401'
        '404':
          description: 404 Not Found.
          content:
            application/json:
              schema:
                $ref: '../../../../api_doc.yaml#/components/schemas/HTTP404'