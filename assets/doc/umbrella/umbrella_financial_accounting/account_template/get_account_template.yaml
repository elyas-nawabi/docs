    post:
      tags:
        - account template
      summary: Get list of account templates.
      description: Get list of account templates.
      operationId: getAccounttemplates
      requestBody:
        description: If you wish to apply filtering, please set the criteria; otherwise, send an empty JSON request.
        content:
          application/json:
            schema:
              type: object
              $ref: '../../../api_doc.yaml#/components/schemas/Filter'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                  type: object
                  properties:
                    data:
                      type: array
                      properties:
                        childs:
                          type: array
                          properties:
                            childs:
                              type: array
                              properties:
                                childs:
                                  type: array
                                  properties:
                                    childs: 
                                      type: array
                                    id:
                                      type: integer
                                    rec_name: 
                                      type: string
                                id: 
                                  type: integer
                                rec_name: 
                                  type: string
                            id: 
                              type: integer
                            rec_name: 
                              type: string
                        id: 
                          type: integer
                        rec_name: 
                          type: string  
                    errors:
                      type: object
                    pagination:
                      properties:
                        page:
                          type: integer
                        per_page:
                          type: integer
                        total_pages:
                          type: integer
                        total_records:
                          type: integer
                    status:
                      type: integer
                    success_message:
                      type: string
                      nullable: true
              example:
                data:
                  childs:
                  - childs:
                    - childs:
                      - childs: []
                        id: 2250
                        rec_name: 100 - Capital social
                      id: 2093
                      rec_name: 10 - Capital
                    id: 2089
                    rec_name: 4 - Acreedores y deudores por operaciones comerciales
                  id: 2085
                  rec_name: 0 - Plan Contable para PYMES 2008
                errors: null
                pagination:
                  page: 1
                  per_page: 100
                  total_pages: 1
                  total_records: 12
                status: 200
                success_message: null
        '401':
          description: >-
            401 Unauthorized: The server could not verify that you are
            authorized to access the URL requested. You either supplied the
            wrong credentials (e.g. a bad password), or your browser doesn't
            understand how to supply the credentials required.
          content:
            application/json:
              schema:
                $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'
    
    