openapi: 3.1.0
servers:
  - url: 'http://api-alpha.umbrellaerp.com'
tags:
  - name: umbrella_planning
    description: >
      Planning base module (Tryton). This module is an integration layer and does
      not define dedicated REST endpoints in the Umbrella API.

      Use the generic Umbrella endpoints (e.g. model CRUD and execute/on_change)
      documented under the "Umbrella" section.
paths: {}
