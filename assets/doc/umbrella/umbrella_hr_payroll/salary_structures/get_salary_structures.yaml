post:
  tags:
    - salary_structures
  summary: Search Salary Structures.
  description: Search Salary Structures.
  operationId: searchSalarystructures
  requestBody:
    description: Optional filter criteria; send an empty JSON object for all records.
    content:
      application/json:
        schema:
          $ref: '../../../api_doc.yaml#/components/schemas/Filter'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: 'schema.yaml'
    '401':
      description: Unauthorized.
      content:
        application/json:
          schema:
            $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'

