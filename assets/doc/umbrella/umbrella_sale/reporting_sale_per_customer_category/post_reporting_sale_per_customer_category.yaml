    post:
      tags:
        - reporting sale per customer category
      summary: Get reporting sale per customer category details.
      description: Get reporting sale per customer category details.
      operationId: getReportingSalePerCustomerCategory
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                context:
                  type: object
                  properties:
                    company: 
                      type: integer
                    period:
                      type: string
                    supplier:
                      type: integer
            example:
              context:
                company: 1
                period: Day
                supplier: 1
      responses:
        '200':
          description: List of reporting sale per customer category.
          content:
            application/json:
              schema:
                  type: object
                  properties:
                    data:
                      type: array
                    errors:
                      type: object
                      nullable: true
                    pagination:
                      type: object
                      properties:
                        page:
                          type: integer
                        per_page:
                          type: integer
                        total_pages:
                          type: integer
                        total_records:
                          type: integer
                    status:
                      type: integer
                    success_message:
                      type: string
                      nullable: true
              example:
                data: []
                error: null
                pagination:
                  page: 1
                  per_page: 100
                  total_pages: 1
                  total_records: 9
                status: 200
                success_message: null
        '401':
          description: >-
            401 Unauthorized: The server could not verify that you are
            authorized to access the URL requested. You either supplied the
            wrong credentials (e.g. a bad password), or your browser doesn't
            understand how to supply the credentials required.
          content:
            application/json:
              schema:
                $ref: '../../../api_doc.yaml#/components/schemas/HTTP401'
    
    